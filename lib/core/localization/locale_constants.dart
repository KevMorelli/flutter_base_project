import 'package:flutter/material.dart';

class LocaleConstants {
  static const supportedLocales = [Locale('en'), Locale('es')];
  static const fallbackLocale = Locale('en');
  static const languagesPath = 'assets/languages';
}
